spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/refermedb
    username: longchu
    password: longchu
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update